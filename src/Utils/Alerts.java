package Utils;

import javafx.scene.control.Alert;

/**
 * This class creates to static alerts to avoid typing.
 */
public class Alerts {
    public static Alert informationAlert = new Alert(Alert.AlertType.INFORMATION);
    public static Alert choiceAlert = new Alert((Alert.AlertType.CONFIRMATION));
}
